import { Children as i } from "react";
function m(e) {
  return i.map(e, ({ props: t }) => t).map(({ name: t, path: a }) => ({
    name: t,
    path: a,
    params: {},
    activePath: a
  }));
}
export {
  m as default
};

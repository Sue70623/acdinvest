import { ReactElement } from 'react';
import { RouteProps } from '../core/Route';
export default function getActivities(children: ReactElement<RouteProps> | ReactElement<RouteProps>[]): {
    name: string;
    path: string;
    params: {};
    activePath: string;
}[];

import { Dispatch } from 'react';
import { ActivityAction, ActivityState } from './typing';
interface ActivityContextProps {
    state: ActivityState;
    dispatch: Dispatch<ActivityAction>;
}
declare const ActivityContext: import('react').Context<ActivityContextProps>;
export default ActivityContext;

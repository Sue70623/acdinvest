import { PropsWithChildren, ReactElement } from 'react';
import { RouteProps } from '../Route';
import { RouterProps } from '../Router';
interface ActivityProviderProps extends Pick<RouterProps, "initPath"> {
    navigates: ReactElement<RouteProps> | ReactElement<RouteProps>[];
}
declare function ActivityProvider({ children, navigates, initPath }: PropsWithChildren<ActivityProviderProps>): import("react").JSX.Element;
export default ActivityProvider;

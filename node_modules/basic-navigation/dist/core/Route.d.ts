import { ReactNode } from 'react';
import { BaseActivity, BaseActivityPath } from './activity/typing';
export interface RouteProps<T extends BaseActivity["name"] = BaseActivity["name"]> {
    name: T;
    path: BaseActivityPath[T];
    params?: Record<string, string>;
    activePath?: string;
    activity: ReactNode;
}
declare function Route<T extends BaseActivity["name"]>({ name, path, activePath, activity }: RouteProps<T>): import("react").JSX.Element | null;
export default Route;

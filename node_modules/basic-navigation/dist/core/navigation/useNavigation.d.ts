import { BaseActivity, BaseActivityParams } from '../activity/typing';
interface Options {
    animate?: boolean;
}
export default function useNavigation(): {
    push: <T extends BaseActivity["name"]>(name: T, params?: BaseActivityParams[T], { animate }?: Options) => void;
    stackPush: <T extends BaseActivity["name"]>(_: T, params?: Partial<BaseActivityParams[T]>, { animate }?: Options) => void;
    replace: <T extends BaseActivity["name"]>(name: T, params?: BaseActivityParams[T], { animate }?: Options) => void;
    back: () => void;
};
export {};

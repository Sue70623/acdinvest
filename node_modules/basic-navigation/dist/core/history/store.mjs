import { HistoryActionType as e } from "./typing.mjs";
function n(r, c) {
  switch (c.type) {
    case e.PUSH:
      return {
        index: r.index + 1,
        records: r.records.concat(c)
      };
    case e.STACK_PUSH:
      return {
        index: r.index + 1,
        records: r.records.concat(c)
      };
    case e.REPLACE:
      return r.records.splice(r.records.length - 2, 1), {
        index: r.index - 1,
        records: r.records
      };
    case e.BACK:
      return {
        index: r.index - 1,
        records: r.records.slice(0, r.records.length - 1)
      };
    default:
      return r;
  }
}
export {
  n as historyReducer
};

import { jsx as p } from "react/jsx-runtime";
import { useContext as a, Children as i, isValidElement as s, cloneElement as m } from "react";
import { d as u } from "../vendor-BPaWSzH4.mjs";
import f from "../history/HistoryContext.mjs";
import l from "./RendererProvider.mjs";
function C({ children: o }) {
  const {
    state: { records: n }
  } = a(f);
  return Array.from(
    new Map(
      n.map((t) => {
        const [r] = t.path.split("?");
        return [r, { ...t, path: t.path }];
      })
    ).values()
  ).map(
    ({ path: t, params: r }) => i.map(o, (e) => s(e) && u.pathToRegexp(e.props.path).regexp.test(t) ? /* @__PURE__ */ p(l, { params: r, children: m(e, { ...e.props, params: r, activePath: t }) }) : null)
  );
}
export {
  C as default
};
